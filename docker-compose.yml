services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: 1Qwerty
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    restart: always

  service:
    build: .
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 1G
    ports:
      - "8082:8082"
    volumes:
      - ./configs:/configs
      - ./.env.docker:/.env
      - ./migrations:/migrations
    depends_on:
      - postgres
    restart: always
